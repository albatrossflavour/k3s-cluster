---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minecraft-data
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-block-long-term
  resources:
    requests:
      storage: 5Gi
